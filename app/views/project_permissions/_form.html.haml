= simple_form_for(@project_permission) do |f|
  = f.error_notification

  .form-inputs
    = f.input :user_id
    = f.input :project_id
    = f.input :abilities
    = f.input :expiration

  .form-actions
    = f.button :submit
