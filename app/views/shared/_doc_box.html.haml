.col-xs-12.doc-box
  .col-xs-12
    %table.table.table-striped
      - @documents.each do |d|
        %tr
          %td
            %span.date= d.created_at.strftime("%b %d, %Y")
            %span= link_to doc_title(d), document_path(d)

